services:
  - type: web
    name: petcare
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py makemigrations
      python manage.py migrate
    startCommand: gunicorn petcare.wsgi:application
    plan: free
    autoDeploy: true
